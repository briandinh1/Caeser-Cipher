Encrypt and decrypt plaintext using caeser-cipher encryption method.
Uses an array to keep track of the original alphabet, and another rotated array that keeps track of the cipher (number of times shifted can be input by the user)
Console based program

An infinite loop can occur if the shift inputed is not an int